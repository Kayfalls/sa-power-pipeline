[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sa-power-pipeline"
version = "0.1.0"
description = "Batch ELT pipeline for South African load shedding data"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]